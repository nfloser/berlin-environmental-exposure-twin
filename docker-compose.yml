services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
  ui:
    image: nginx:alpine
    volumes:
      - ./frontend/dist:/usr/share/nginx/html:ro
    ports:
      - "8080:80"
    depends_on:
      - api
